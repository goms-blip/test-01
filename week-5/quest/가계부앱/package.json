{
  "name": "ledger-app",
  "version": "1.0.0",
  "private": true,
  "description": "나만의 가계부 앱 — Express + Supabase(PostgreSQL)",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
